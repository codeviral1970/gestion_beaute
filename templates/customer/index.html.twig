{% extends 'base.html.twig' %}

{% block body %}

<div class="container mx-auto mt-4">
    <div class="card text-center">
		<h1>Liste des clients</h1>
    </div>
    <div class="row mt-4">
		  {# {{component('customer', {id : 41})}} #}

		  {{component('all_customers')}}
    </div>
</div>
{% endblock %}