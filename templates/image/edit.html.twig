{% extends 'base.html.twig' %}

{% block title %}Image | Edition {% endblock %}

{% block body %}
		<!-- Card with header and footer -->
	<div class="card">
		<div class="card-header">Editer une photo</div>
		<div class="card-body">
			<h5 class="card-title"></h5>
			{{form_start(form)}}
			{{form_widget(form)}}
			{{form_end(form)}}
		</div>
		<div class="card-footer">
			Footer
		</div>
	</div><!-- End Card with header and footer -->
{% endblock %}
